<?php

class Cm_attributesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('cm_attributes')->truncate();

		$cm_attributes = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('cm_attributes')->insert($cm_attributes);
	}

}
